# ********RoostGPT********

# Test generated by RoostGPT for test testUnitNjs2 using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /pet/findByTags_get for http method type GET in artillery framework
# RoostTestHash=c791547528
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "pet_findByTags_get_others.csv"
    fields: ["tags"]
    skipHeader: true
    delimiter: "^|^"
  environments:
    dev:
      target: "{{ $processEnvironment.API_HOST }}"
      http:
        timeout: 10

scenarios:
  - name: "Non-success API requests"
    flow:
      - log: "Sending a GET request with invalid token"
      - get:
          url: "/v2/pet/findByTags?tags={{ tags }}"
          headers:
            api_key: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 401
            - hasProperty: "message"
      - think: 1
      - log: "Sending a GET request with non-existing tags"
      - get:
          url: "/v2/pet/findByTags?tags=nonexistingtag"
          headers:
            api_key: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasProperty: "message"
      - think: 1
      - log: "Sending a GET request with incorrect URL"
      - get:
          url: "/v2/pet/incorrectUrl?tags={{ tags }}"
          headers:
            api_key: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasProperty: "message"
      - think: 1
