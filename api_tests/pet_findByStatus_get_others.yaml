# ********RoostGPT********

# Test generated by RoostGPT for test testUnitNjs2 using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /pet/findByStatus_get for http method type GET in artillery framework
# RoostTestHash=9575439991
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "pet_findByStatus_get_others.csv"
    skipHeader: true
    fields:
      - status
    delimiter: "^|^"
  phases:
    - duration: 60
      arrivalRate: 20
scenarios:
  - name: "Non-successful API requests"
    flow:
      - log: "GET request to /v2/pet/findByStatus with invalid status"
      - get:
          url: "/v2/pet/findByStatus?status={{ status }}"
          headers:
            api_key: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "message"
            - hasProperty: "code"
      - think: 1
      - log: "GET request to /v2/pet/findByStatus without authorization"
      - get:
          url: "/v2/pet/findByStatus?status=available"
          expect:
            - statusCode: 401
      - think: 1
      - log: "GET request to an incorrect URL"
      - get:
          url: "/v2/pet/incorrectUrl"
          headers:
            api_key: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
      - think: 1
