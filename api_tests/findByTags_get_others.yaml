# Test generated by RoostGPT for test testUnitNjs2 using AI Type Open AI and AI Model gpt-4


# Test generated for /pet/findByTags_get for http method type GET in artillery framework

# RoostTestHash=c791547528


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "findByTags_get_others.csv"
    fields:
      - "tags"
    skipHeader: true
  http:
    pool: 10
  phases:
    - duration: 60
      arrivalRate: 20
scenarios:
  - name: "Non-success API requests"
    flow:
      - log: "Sending a request with invalid authorization token"
        post:
          url: "/v2/pet/findByTags"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          json:
            tags: ["invalid_tag"]
          expect:
            - statusCode: 401
            - hasProperty: "message"
        think: 1

      - log: "Sending a request with missing tags parameter"
        post:
          url: "/v2/pet/findByTags"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "message"
        think: 1

      - log: "Sending a request to an invalid endpoint"
        post:
          url: "/invalid_endpoint"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
        think: 1
