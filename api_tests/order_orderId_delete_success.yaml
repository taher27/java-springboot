# Test generated by RoostGPT for test testUnitNjs2 using AI Type Open AI and AI Model gpt-4


# Test generated for /store/order/{orderId}_delete for http method type DELETE in artillery framework

# RoostTestHash=6e4c4bbafe


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "order_orderId_delete_success.csv"
    fields:
      - "orderId"
    skipHeader: true

scenarios:
  - name: Delete order API test scenario
    flow:
      - log: "Starting test scenario - Delete order"
      - delete:
           url: "/v2/store/order/{{ orderId }}"
           headers:
             "{{ security.name }}": "{{$processEnvironment.AUTH_TOKEN}}"
           expect:
             - statusCode: 200
             - hasHeader: "Content-Type"
             - headerEquals: {"Content-Type": "application/json"}
      - think: 2
      - log: "Completed test scenario - Delete order"
