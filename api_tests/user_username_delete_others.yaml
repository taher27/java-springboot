# Test generated by RoostGPT for test testUnitNjs2 using AI Type Open AI and AI Model gpt-4


# Test generated for /user/{username}_delete for http method type DELETE in artillery framework

# RoostTestHash=d8125b8dc9


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "user_username_delete_others.csv"
    fields:
      - "username"
    skipHeader: true
  environments:
    dev:
      target: "http://dev.api.host"
      $processEnvironment:
        AUTH_TOKEN: "YOUR_AUTH_TOKEN"

scenarios:
  - name: "Delete user API test"
    flow:
      - log: "Testing with invalid authorization token"
        post:
          url: "/v2/user/{{ username }}"
          headers:
            Authorization: "InvalidToken"
          expect:
            - statusCode: 401 # Unauthorized
            - hasProperty: "message"
      - think: 1
      - log: "Testing with missing authorization token"
        post:
          url: "/v2/user/{{ username }}"
          expect:
            - statusCode: 401 # Unauthorized
            - hasProperty: "message"
      - think: 1
      - log: "Testing with invalid username"
        post:
          url: "/v2/user/invalidUsername"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404 # Not Found
            - hasProperty: "message"
      - think: 1
      - log: "Testing with missing username"
        post:
          url: "/v2/user/"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400 # Bad Request
            - hasProperty: "message"
      - think: 1
      - log: "Testing with incorrect HTTP method"
        get:
          url: "/v2/user/{{ username }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 405 # Method Not Allowed
            - hasProperty: "message"
      - think: 1
