# ********RoostGPT********

# Test generated by RoostGPT for test testUnitNjs2 using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /pet/{petId}_get for http method type GET in artillery framework
# RoostTestHash=4dfd26d5a8
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "pet_petId_get_others.csv"
    fields: ["petId"]
    skipHeader: true
    delimiter: "^|^"
  headers:
    api_key: "{{$processEnvironment.AUTH_TOKEN}}"

scenarios:
  - name: "Non-Success API Responses Test"
    flow:
      - log: "Executing GET request with invalid petId"
      - get:
          url: "/v2/pet/{{ petId }}"
          expect:
            - statusCode: 404
            - hasProperty: "message"
            - equals: ["message", "Pet not found"]
      - think: 1

      - log: "Executing GET request without authorization"
      - get:
          url: "/v2/pet/{{ petId }}"
          headers:
            api_key: ""
          expect:
            - statusCode: 401
            - hasProperty: "message"
            - equals: ["message", "Unauthorized"]
      - think: 1

      - log: "Executing GET request with invalid URL"
      - get:
          url: "/v2/invalid/{{ petId }}"
          expect:
            - statusCode: 404
      - think: 1
