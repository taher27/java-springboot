# Test generated by RoostGPT for test testUnitNjs2 using AI Type Open AI and AI Model gpt-4


# Test generated for /pet/{petId}/uploadImage_post for http method type POST in artillery framework

# RoostTestHash=354b987cbe


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "uploadImage_post_others.csv"
    fields:
      - "petId"
      - "additionalMetadata"
      - "file"
    skipHeader: true
  phases:
    - duration: 60
      arrivalRate: 20

scenarios:
  - name: "Non-Success API Requests"
    flow:
      - log: "Testing with incorrect petId"
        post:
          url: "/v2/pet/{{ petId }}/uploadImage"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          formData:
            additionalMetadata: "{{ additionalMetadata }}"
            file: "{{ file }}"
          expect:
            - statusCode: 404 # Expecting a 404 response as the petId is incorrect
        think: 1

      - log: "Testing with missing file"
        post:
          url: "/v2/pet/{{ petId }}/uploadImage"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          formData:
            additionalMetadata: "{{ additionalMetadata }}"
          expect:
            - statusCode: 400 # Expecting a 400 response as the file is missing
        think: 1

      - log: "Testing with incorrect API URL"
        post:
          url: "/v2/invalid/{{ petId }}/uploadImage"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          formData:
            additionalMetadata: "{{ additionalMetadata }}"
            file: "{{ file }}"
          expect:
            - statusCode: 404 # Expecting a 404 response as the API URL is incorrect
        think: 1

      - log: "Testing with incorrect authorization token"
        post:
          url: "/v2/pet/{{ petId }}/uploadImage"
          headers:
            Authorization: "Bearer invalid_token"
          formData:
            additionalMetadata: "{{ additionalMetadata }}"
            file: "{{ file }}"
          expect:
            - statusCode: 401 # Expecting a 401 response as the authorization token is incorrect
        think: 1
