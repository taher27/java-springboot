# ********RoostGPT********

# Test generated by RoostGPT for test testUnitNjs2 using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /pet_put for http method type PUT in artillery framework
# RoostTestHash=04bbbce6b4
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  ensure:
    p99: 200
  plugins:
    expect: {}
  payload:
    path: "pet_put_others.csv"
    fields: ["id", "name", "photoUrls", "tags", "status"]
    skipHeader: true
    delimiter: "^|^"

scenarios:
  - name: "Update pet test scenario"
    flow:
      - log: "Attempting to update pet with invalid payload"
      - post:
          url: "/v2/pet"
          headers:
            api_key: "{{$processEnvironment.AUTH_TOKEN}}"
          json:
            id: "{{ id }}"
            name: "{{ name }}"
            photoUrls: "{{ photoUrls }}"
            tags: "{{ tags }}"
            status: "{{ status }}"
          expect:
            - statusCode: 400
            - hasProperty: "message"
      - think: 1

      - log: "Attempting to update pet with incorrect URL"
      - post:
          url: "/v2/invalid_pet"
          headers:
            api_key: "{{$processEnvironment.AUTH_TOKEN}}"
          json:
            id: "{{ id }}"
            name: "{{ name }}"
            photoUrls: "{{ photoUrls }}"
            tags: "{{ tags }}"
            status: "{{ status }}"
          expect:
            - statusCode: 404
      - think: 1

      - log: "Attempting to update pet without authorization"
      - post:
          url: "/v2/pet"
          json:
            id: "{{ id }}"
            name: "{{ name }}"
            photoUrls: "{{ photoUrls }}"
            tags: "{{ tags }}"
            status: "{{ status }}"
          expect:
            - statusCode: 401
            - hasProperty: "message"
      - think: 1
