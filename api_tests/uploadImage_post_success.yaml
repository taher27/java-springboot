# Test generated by RoostGPT for test testUnitNjs2 using AI Type Open AI and AI Model gpt-4


# Test generated for /pet/{petId}/uploadImage_post for http method type POST in artillery framework

# RoostTestHash=354b987cbe


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 10
  payload:
    path: "uploadImage_post_success.csv"
    fields:
      - "petId"
      - "additionalMetadata"
      - "file"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Upload Image Test Scenario"
    flow:
      - post:
          url: "/v2/pet/{{ petId }}/uploadImage"
          headers:
            petstore_auth: "{{$processEnvironment.AUTH_TOKEN}}"
          formData:
            additionalMetadata: "{{ additionalMetadata }}"
            file: "{{ file }}"
          expect:
            - statusCode: 200
            - hasProperty: "code"
            - hasProperty: "type"
            - hasProperty: "message"
          # Log statement indicating successful image upload
      - log: "Image uploaded successfully"
      - think: 1
