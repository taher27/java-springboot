# ********RoostGPT********

# Test generated by RoostGPT for test testUnitNjs2 using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /pet/{petId}_post for http method type POST in artillery framework
# RoostTestHash=99d9f02040
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "pet_petId_post_success.csv"
    fields:
      - "petId"
      - "name"
      - "status"
    skipHeader: true
    delimiter: "^|^"
  environments:
    dev:
      target: "{{ $processEnvironment.API_HOST }}"
    
scenarios:
  - name: "Test successful API responses"
    flow:
      - log: "Starting test scenario for updating a pet with form data"
      - post:
          url: "/v2/pet/{{ petId }}"
          headers:
            api_key: "{{$processEnvironment.AUTH_TOKEN}}"
          formData:
            name: "{{ name }}"
            status: "{{ status }}"
          expect:
            - statusCode: 200
            - hasHeader: "content-type"
            - hasProperty: "name"
            - hasProperty: "status"
      - equals: 
          - "name"
          - "{{ name }}"
      - equals: 
          - "status"
          - "{{ status }}"
      - capture:
          json: "$.name"
          as: "name"
      - capture:
          json: "$.status"
          as: "status"
      - think: 1
