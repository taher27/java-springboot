# ********RoostGPT********

# Test generated by RoostGPT for test testUnitNjs2 using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /pet/findByStatus_get for http method type GET in artillery framework
# RoostTestHash=9575439991
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "pet_findByStatus_get_success.csv"
    fields: ["status"]
    skipHeader: true
    delimiter: "^|^"

scenarios:
  - name: "Pet Store API Test Scenario"
    flow:
      - log: "Starting test for /v2/pet/findByStatus"
      - get:
          url: "/v2/pet/findByStatus?status={{ status }}"
          headers:
            api_key: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "id"
            - hasProperty: "category"
            - hasProperty: "name"
            - hasProperty: "photoUrls"
            - hasProperty: "tags"
            - hasProperty: "status"
      - think: 
          duration: 1
