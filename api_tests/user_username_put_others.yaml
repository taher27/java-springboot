# Test generated by RoostGPT for test testUnitNjs2 using AI Type Open AI and AI Model gpt-4


# Test generated for /user/{username}_put for http method type PUT in artillery framework

# RoostTestHash=f6e646c39e


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "user_username_put_others.csv"
    fields:
      - "username"
      - "firstName"
      - "lastName"
      - "email"
      - "password"
      - "phone"
      - "userStatus"
    skipHeader: true
  plugins:
    expect: {}
scenarios:
  - name: "Non-success API responses testing"
    flow:
      - put:
          url: "/v2/user/{{ username }}"
          headers:
            Content-Type: "application/json"
            {{security_scheme_name}}: "{{$processEnvironment.AUTH_TOKEN}}"
          json:
            id: 0
            username: "{{ username }}"
            firstName: "{{ firstName }}"
            lastName: "{{ lastName }}"
            email: "{{ email }}"
            password: "{{ password }}"
            phone: "{{ phone }}"
            userStatus: "{{ userStatus }}"
          expect:
            - statusCode: 400
            - hasProperty: "message"
            - equals: ["message", "Invalid request body"]
          capture:
            - json: "$.message"
              as: "responseMessage"
        log: "Invalid request body"
      - think: 1
      - put:
          url: "/v2/user/{{ username }}"
          headers:
            Content-Type: "application/json"
            {{security_scheme_name}}: "{{$processEnvironment.AUTH_TOKEN}}"
          json:
            id: "{{ id }}"
            username: "{{ username }}"
            firstName: "{{ firstName }}"
            lastName: "{{ lastName }}"
            email: "{{ email }}"
            password: "{{ password }}"
            phone: "{{ phone }}"
            userStatus: "{{ userStatus }}"
          expect:
            - statusCode: 401
            - hasProperty: "message"
            - equals: ["message", "Unauthorized"]
          capture:
            - json: "$.message"
              as: "responseMessage"
        log: "Unauthorized request"
      - think: 1
      - put:
          url: "/v2/user/nonexistentuser"
          headers:
            Content-Type: "application/json"
            {{security_scheme_name}}: "{{$processEnvironment.AUTH_TOKEN}}"
          json:
            id: "{{ id }}"
            username: "{{ username }}"
            firstName: "{{ firstName }}"
            lastName: "{{ lastName }}"
            email: "{{ email }}"
            password: "{{ password }}"
            phone: "{{ phone }}"
            userStatus: "{{ userStatus }}"
          expect:
            - statusCode: 404
            - hasProperty: "message"
            - equals: ["message", "User not found"]
          capture:
            - json: "$.message"
              as: "responseMessage"
        log: "Nonexistent user"
      - think: 1
