# ********RoostGPT********

# Test generated by RoostGPT for test testUnitNjs2 using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /pet/findByTags_get for http method type GET in artillery framework
# RoostTestHash=c791547528
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "pet_findByTags_get_success.csv"
    fields: ["tags"]
    skipHeader: true
    delimiter: "^|^"

scenarios:
  - name: "Find Pets by tags"
    flow:
      - log: "Starting test for /v2/pet/findByTags"
      - get:
          url: "/v2/pet/findByTags?tags={{ tags }}"
          headers:
            api_key: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "id"
            - hasProperty: "category"
            - hasProperty: "name"
            - hasProperty: "photoUrls"
            - hasProperty: "tags"
            - hasProperty: "status"
            - equals:
                - "{{ $response.body.name }}"
                - "doggie"
      - think: 1
