# Test generated by RoostGPT for test testUnitNjs2 using AI Type Open AI and AI Model gpt-4


# Test generated for /store/inventory_get for http method type GET in artillery framework

# RoostTestHash=a66f7079af


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 10
  plugins:
    expect: {}
  payload:
    path: "inventory_get_success.csv"
    fields:
      - "field1"
      - "field2"
    skipHeader: true
  headers:
    api_key: "{{$processEnvironment.AUTH_TOKEN}}"

scenarios:
  - name: "Test API behavior with valid inputs"
    flow:
      - get:
          url: "/v2/store/inventory"
          headers:
            api_key: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 200
            - hasProperty: "field1"
            - hasProperty: "field2"
          capture:
            - json: "$.field1"
              as: "response_field1"
            - json: "$.field2"
              as: "response_field2"
      - log: "Received successful response for GET /v2/store/inventory"
      - think: 1
