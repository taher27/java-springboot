# Test generated by RoostGPT for test testUnitNjs2 using AI Type Open AI and AI Model gpt-4


# Test generated for /user/{username}_delete for http method type DELETE in artillery framework

# RoostTestHash=d8125b8dc9


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "user_username_delete_success.csv"
    fields:
      - "username"
    skipHeader: true
  headers:
    SECURITY_SCHEMA: "{{ $processEnvironment.AUTH_TOKEN }}"
scenarios:
  - name: Delete User Scenario
    flow:
      - log: "Delete User Request"
      - delete:
          url: "/v2/user/{{ username }}"
          headers:
            SECURITY_SCHEMA: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            # Assert response header
            - hasHeader: "Content-Type"
            - headerEquals: { "Content-Type": "application/json" }
            # Assert response body
            - hasProperty: "message"
            - equals: { "message": "User deleted successfully." }
      - think: 1
