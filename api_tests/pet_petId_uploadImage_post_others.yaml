# ********RoostGPT********

# Test generated by RoostGPT for test testUnitNjs2 using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /pet/{petId}/uploadImage_post for http method type POST in artillery framework
# RoostTestHash=354b987cbe
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "pet_petId_uploadImage_post_others.csv"
    fields:
      - "petId"
      - "additionalMetadata"
      - "file"
    skipHeader: true
    delimiter: "^|^"

scenarios:
  - name: "Non-Success API Responses"
    flow:
      - log: "POST request to /v2/pet/{petId}/uploadImage with incorrect payload"
      - post:
          url: "/v2/pet/{{ petId }}/uploadImage"
          headers:
            api_key: "{{ $processEnvironment.AUTH_TOKEN }}"
          formData:
            additionalMetadata: "{{ additionalMetadata }}"
            file: "{{ file }}"
          expect:
            - statusCode: 400
            - hasProperty: "message"
            - equals: 
                - "message"
                - "Invalid input"
      - think: 1

      - log: "POST request to /v2/pet/{petId}/uploadImage with incorrect URL"
      - post:
          url: "/v2/pet/incorrect/url"
          headers:
            api_key: "{{ $processEnvironment.AUTH_TOKEN }}"
          formData:
            additionalMetadata: "{{ additionalMetadata }}"
            file: "{{ file }}"
          expect:
            - statusCode: 404
      - think: 1

      - log: "POST request to /v2/pet/{petId}/uploadImage with invalid authentication"
      - post:
          url: "/v2/pet/{{ petId }}/uploadImage"
          headers:
            api_key: "invalid_auth_token"
          formData:
            additionalMetadata: "{{ additionalMetadata }}"
            file: "{{ file }}"
          expect:
            - statusCode: 401
      - think: 1
