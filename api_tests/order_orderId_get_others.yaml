# Test generated by RoostGPT for test testUnitNjs2 using AI Type Open AI and AI Model gpt-4


# Test generated for /store/order/{orderId}_get for http method type GET in artillery framework

# RoostTestHash=3e40e8eac6


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "order_orderId_get_others.csv"
    fields:
      - "orderId"
    skipHeader: true

scenarios:
  - name: "Test for non-success API responses"
    flow:
      - get:
          url: "/v2/store/order/{{ orderId }}"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400 # Expecting a Bad Request error due to invalid orderId
            - hasHeader: "Content-Type"
            - headerEquals: ["Content-Type", "application/json"]
            - hasProperty: "message"
            - equals: ["message", "Invalid ID supplied"]
          think: 1
      - get:
          url: "/v2/store/order/9999999999"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404 # Expecting a Not Found error due to non-existent orderId
            - hasHeader: "Content-Type"
            - headerEquals: ["Content-Type", "application/json"]
            - hasProperty: "message"
            - equals: ["message", "Order not found"]
          think: 1
      - get:
          url: "/v2/store/order/abc"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400 # Expecting a Bad Request error due to non-numeric orderId
            - hasHeader: "Content-Type"
            - headerEquals: ["Content-Type", "application/json"]
            - hasProperty: "message"
            - equals: ["message", "Invalid ID supplied"]
          think: 1
      - get:
          url: "/v2/store/nonexistent/{{ orderId }}"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404 # Expecting a Not Found error due to incorrect URL
            - hasHeader: "Content-Type"
            - headerEquals: ["Content-Type", "application/json"]
            - hasProperty: "message"
            - equals: ["message", "Not found"]
          think: 1
