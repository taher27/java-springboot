# ********RoostGPT********

# Test generated by RoostGPT for test testUnitNjs2 using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /pet/{petId}/uploadImage_post for http method type POST in artillery framework
# RoostTestHash=354b987cbe
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "pet_petId_uploadImage_post_success.csv"
    fields:
      - "petId"
      - "additionalMetadata"
      - "file"
    skipHeader: true
    delimiter: "^|^"

scenarios:
  - name: "Upload an image to a pet"
    flow:
      - log: "Starting test for image upload API"
      - post:
          url: "/v2/pet/{{ petId }}/uploadImage"
          formData:
            additionalMetadata: "{{ additionalMetadata }}"
            file: "{{ file }}"
          headers:
            api_key: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "code"
            - hasProperty: "type"
            - hasProperty: "message"
      - think: 1
