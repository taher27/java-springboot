# Test generated by RoostGPT for test testUnitNjs2 using AI Type Open AI and AI Model gpt-4


# Test generated for /pet/findByStatus_get for http method type GET in artillery framework

# RoostTestHash=9575439991


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 5
  plugins:
    expect: {}
  payload:
    path: "findByStatus_get_others.csv"
    fields:
      - "status"
    skipHeader: true
  defaults:
    headers:
      Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"

scenarios:
  - name: "Non-success API responses"
    flow:
      - log: "Sending GET request with incorrect status"
        get:
          url: "/v2/pet/findByStatus?status={{ status }}"
          expect:
            - statusCode: 400 # Expecting a Bad Request response
            - hasProperty: "message"
            - hasProperty: "code"
        think: 1

      - log: "Sending GET request without authorization header"
        get:
          url: "/v2/pet/findByStatus"
          headers: {} # Removing the authorization header
          expect:
            - statusCode: 401 # Expecting an Unauthorized response
            - hasProperty: "message"
            - hasProperty: "code"
        think: 1

      - log: "Sending GET request to an invalid URL"
        get:
          url: "/v2/pet/findByStatusInvalid"
          expect:
            - statusCode: 404 # Expecting a Not Found response
            - hasProperty: "message"
            - hasProperty: "code"
        think: 1
