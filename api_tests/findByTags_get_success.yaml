# Test generated by RoostGPT for test testUnitNjs2 using AI Type Open AI and AI Model gpt-4


# Test generated for /pet/findByTags_get for http method type GET in artillery framework

# RoostTestHash=c791547528


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "findByTags_get_success.csv"
    fields:
      - "tag"
    skipHeader: true

scenarios:
  - name: "Find Pets by tags"
    flow:
      - log: "Fetching pets by tags"
      - get:
          url: "/v2/pet/findByTags?tags={{ tag }}"
          headers:
            petstore_auth: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - contentType: "application/json"
            - hasProperty: "name"
            - hasProperty: "photoUrls"
      - think: 1
