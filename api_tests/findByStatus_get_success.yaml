# Test generated by RoostGPT for test testUnitNjs2 using AI Type Open AI and AI Model gpt-4


# Test generated for /pet/findByStatus_get for http method type GET in artillery framework

# RoostTestHash=9575439991


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "findByStatus_get_success.csv"
    fields:
      - "status"
    skipHeader: true
  headers:
    Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"

scenarios:
  - name: "Pet Store API Scenario"
    flow:
      # Request to get pets by status
      - get:
          url: "/v2/pet/findByStatus?status={{ status }}"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          capture:
            - json: "$.id"
              as: "petId"
          expect:
            - statusCode: 200
            - hasProperty: "id"
            - hasProperty: "name"
            - hasProperty: "photoUrls"
            - hasProperty: "status"
      - think: 1
