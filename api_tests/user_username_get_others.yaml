# Test generated by RoostGPT for test testUnitNjs2 using AI Type Open AI and AI Model gpt-4


# Test generated for /user/{username}_get for http method type GET in artillery framework

# RoostTestHash=da281fe471


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 10
  payload:
    path: "user_username_get_others.csv"
    fields:
      - "username"
    skipHeader: true
  plugins:
    expect: {}
  processor: "./my_functions.js"

scenarios:
  - name: "Non-success API responses"
    flow:
      - log: "Sending a GET request with a valid username but without authorization"
        get:
          url: "/v2/user/{{ username }}"
        expect:
          - hasProperty: "message"
          - equals: ["statusCode", 401] # expecting Unauthorized
        think: 1

      - log: "Sending a GET request with a valid username and with invalid authorization"
        get:
          url: "/v2/user/{{ username }}"
          headers:
            Authorization: "Bearer invalid_token"
        expect:
          - hasProperty: "message"
          - equals: ["statusCode", 403] # expecting Forbidden
        think: 1

      - log: "Sending a GET request with an invalid username and with valid authorization"
        get:
          url: "/v2/user/invalid_username"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
        expect:
          - hasProperty: "message"
          - equals: ["statusCode", 404] # expecting Not Found
        think: 1

      - log: "Sending a GET request with a valid username and with valid authorization to an incorrect URL"
        get:
          url: "/v2/users/{{ username }}"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
        expect:
          - hasProperty: "message"
          - equals: ["statusCode", 404] # expecting Not Found
        think: 1
