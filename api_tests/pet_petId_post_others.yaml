# Test generated by RoostGPT for test testUnitNjs2 using AI Type Open AI and AI Model gpt-4


# Test generated for /pet/{petId}_post for http method type POST in artillery framework

# RoostTestHash=99d9f02040


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "pet_petId_post_others.csv"
    fields:
      - "petId"
      - "name"
      - "status"
    skipHeader: true
  headers:
    {{ SECURITY_SCHEMA.name }}: "{{$processEnvironment.AUTH_TOKEN}}"
scenarios:
  - name: "Non-Success API Requests"
    flow:
      - post:
          url: "/v2/pet/{{ petId }}"
          headers:
            Content-Type: "application/x-www-form-urlencoded"
            {{ SECURITY_SCHEMA.name }}: "{{$processEnvironment.AUTH_TOKEN}}"
          formData:
            name: "{{ name }}"
            status: "{{ status }}"
          expect:
            - statusCode: 400
            # Expecting a 400 response because the payload is invalid
            - hasProperty: "message"
            # Expecting the response to have a 'message' property indicating the error
          think: 1

      - post:
          url: "/v2/pet/nonexistent"
          headers:
            Content-Type: "application/x-www-form-urlencoded"
            {{ SECURITY_SCHEMA.name }}: "{{$processEnvironment.AUTH_TOKEN}}"
          formData:
            name: "{{ name }}"
            status: "{{ status }}"
          expect:
            - statusCode: 404
            # Expecting a 404 response because the pet does not exist
            - hasProperty: "message"
            # Expecting the response to have a 'message' property indicating the error
          think: 1

      - post:
          url: "/v2/pet/{{ petId }}"
          headers:
            Content-Type: "application/x-www-form-urlencoded"
            {{ SECURITY_SCHEMA.name }}: ""
          formData:
            name: "{{ name }}"
            status: "{{ status }}"
          expect:
            - statusCode: 401
            # Expecting a 401 response because the authentication token is missing
            - hasProperty: "message"
            # Expecting the response to have a 'message' property indicating the error
          think: 1
