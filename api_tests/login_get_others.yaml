# Test generated by RoostGPT for test testUnitNjs2 using AI Type Open AI and AI Model gpt-4


# Test generated for /user/login_get for http method type GET in artillery framework

# RoostTestHash=474b7881ac


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 5
      arrivalRate: 1
  payload:
    path: "login_get_others.csv"
    fields:
      - "username"
      - "password"
    skipHeader: true
  plugins:
    expect: {}
scenarios:
  - name: "Non-Success API Requests"
    flow:
      - log: "Invalid login attempt with wrong credentials"
        get:
          url: "/v2/user/login?username={{ username }}&password={{ password }}"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 401
      - think: 1

      - log: "Invalid login attempt with empty credentials"
        get:
          url: "/v2/user/login?username=&password="
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
      - think: 1

      - log: "Login attempt without authorization header"
        get:
          url: "/v2/user/login?username={{ username }}&password={{ password }}"
          expect:
            - statusCode: 403
      - think: 1

      - log: "Login attempt with incorrect URL"
        get:
          url: "/v3/user/login?username={{ username }}&password={{ password }}"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
      - think: 1
