# Test generated by RoostGPT for test testUnitNjs2 using AI Type Open AI and AI Model gpt-4


# Test generated for /user/logout_get for http method type GET in artillery framework

# RoostTestHash=a64e3feb39


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "logout_get_others.csv"
    fields: ["username", "password"]
    skipHeader: true

scenarios:
  - name: "Non-Success API Responses"
    flow:
      - log: "Sending an invalid POST request with missing payload"
        post:
          url: "/v2/user/logout"
          headers:
            authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
            content-type: "application/json"
          json:
            username: "{{ username }}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
        think: 1

      - log: "Sending a GET request with incorrect URL"
        get:
          url: "/v2/user/logout/invalid"
          headers:
            authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
            content-type: "application/json"
          expect:
            - statusCode: 404
        think: 1

      - log: "Sending a GET request without authorization token"
        get:
          url: "/v2/user/logout"
          headers:
            content-type: "application/json"
          expect:
            - statusCode: 401
        think: 1

      - log: "Sending a DELETE request to a GET endpoint"
        delete:
          url: "/v2/user/logout"
          headers:
            authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
            content-type: "application/json"
          expect:
            - statusCode: 405
        think: 1
