# ********RoostGPT********

# Test generated by RoostGPT for test testUnitNjs2 using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /pet/{petId}_delete for http method type DELETE in artillery framework
# RoostTestHash=fbd3e66b3b
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "pet_petId_delete_others.csv"
    fields: ["petId"]
    skipHeader: true
    delimiter: "^|^"

scenarios:
  - name: "Non-Success API Responses"
    flow:
      - log: "Delete Pet With Invalid API Key"
      - delete:
          url: "/v2/pet/{{ petId }}"
          headers:
            api_key: "InvalidAPIKey"
          expect:
            - statusCode: 401
      - think: 1

      - log: "Delete Pet With Empty API Key"
      - delete:
          url: "/v2/pet/{{ petId }}"
          headers:
            api_key: ""
          expect:
            - statusCode: 401
      - think: 1

      - log: "Delete Pet With Missing API Key"
      - delete:
          url: "/v2/pet/{{ petId }}"
          expect:
            - statusCode: 401
      - think: 1

      - log: "Delete Pet With Nonexistent PetId"
      - delete:
          url: "/v2/pet/0"
          headers:
            api_key: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 404
      - think: 1

      - log: "Delete Pet With Invalid URL"
      - delete:
          url: "/invalid/url/{{ petId }}"
          headers:
            api_key: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 404
      - think: 1
