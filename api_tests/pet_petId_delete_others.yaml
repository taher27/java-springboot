# Test generated by RoostGPT for test testUnitNjs2 using AI Type Open AI and AI Model gpt-4


# Test generated for /pet/{petId}_delete for http method type DELETE in artillery framework

# RoostTestHash=fbd3e66b3b


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "pet_petId_delete_others.csv"
    fields:
      - "petId"
      - "api_key"
    skipHeader: true

scenarios:
  - name: "Non-success API responses"
    flow:
      - delete:
          url: "/v2/pet/{{ petId }}"
          headers:
            api_key: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 401
            - hasHeader: "Invalid API key"
          # Expecting a 401 status code and an 'Invalid API key' error message when an invalid API key is used
          think: 1

      - delete:
          url: "/v2/pet/12345"
          headers:
            api_key: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 404
            - hasProperty: "message"
            - equals: ["message", "Pet not found"]
          # Expecting a 404 status code and a 'Pet not found' error message when a non-existing petId is used
          think: 1

      - delete:
          url: "/v2/pet/{{ petId }}"
          headers:
            api_key: "invalid_token"
          expect:
            - statusCode: 403
            - hasProperty: "message"
            - equals: ["message", "Forbidden"]
          # Expecting a 403 status code and a 'Forbidden' error message when an invalid token is used
          think: 1
