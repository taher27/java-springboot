# Test generated by RoostGPT for test testUnitNjs2 using AI Type Open AI and AI Model gpt-4


# Test generated for /pet_post for http method type POST in artillery framework

# RoostTestHash=11a919de45


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "pet_post_success.csv"
    fields:
      - "id"
      - "name"
      - "photoUrls"
      - "status"
    skipHeader: true
  environments:
    dev:
      target: "https://dev.example.com"
      headers:
        Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"

scenarios:
  - name: "Add a new pet to the store"
    flow:
      - log: "Starting test scenario: Add a new pet to the store"
      - post:
          url: "/v2/pet"
          headers:
            Content-Type: "application/json"
          json:
            id: "{{ id }}"
            name: "{{ name }}"
            photoUrls: "{{ photoUrls }}"
            status: "{{ status }}"
          expect:
            - statusCode: 200
            - hasProperty: "id"
            - hasProperty: "name"
            - hasProperty: "photoUrls"
            - hasProperty: "status"
      - think: 1
      - log: "Successfully added a new pet to the store"
