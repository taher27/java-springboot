# ********RoostGPT********

# Test generated by RoostGPT for test testUnitNjs2 using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /pet_post for http method type POST in artillery framework
# RoostTestHash=11a919de45
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "pet_post_success.csv"
    fields: ["name", "photoUrls"]
    skipHeader: true
    delimiter: "^|^"
  headers:
    Content-Type: "application/json"
    api_key: "{{ $processEnvironment.AUTH_TOKEN }}"

scenarios:
  - name: "Successful API requests test"
    flow:
      - log: "Starting API tests"
      - post:
          url: "/v2/pet"
          headers:
            api_key: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            name: "{{ name }}"
            photoUrls: "{{ photoUrls }}"
          expect:
            - statusCode: 201
            - hasProperty: "name"
            - hasProperty: "photoUrls"
      - think: 1
