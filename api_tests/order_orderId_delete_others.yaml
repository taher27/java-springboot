# Test generated by RoostGPT for test testUnitNjs2 using AI Type Open AI and AI Model gpt-4


# Test generated for /store/order/{orderId}_delete for http method type DELETE in artillery framework

# RoostTestHash=6e4c4bbafe


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "order_orderId_delete_others.csv"
    fields:
      - "orderId"
    skipHeader: true

scenarios:
  - name: "Test non-successful API requests"
    flow:
      - log: "Sending a DELETE request with an invalid order ID"
        delete:
          url: "/v2/store/order/{{ orderId }}"
          headers:
            {{ $processEnvironment.AUTH_TOKEN }}: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasProperty: "message"
            - equals: 
                - "message": "Order not found"
        think: 1

      - log: "Sending a DELETE request without an order ID"
        delete:
          url: "/v2/store/order/"
          headers:
            {{ $processEnvironment.AUTH_TOKEN }}: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "message"
            - equals:
                - "message": "Bad Request: Order ID is missing"
        think: 1

      - log: "Sending a DELETE request with an unauthorized token"
        delete:
          url: "/v2/store/order/{{ orderId }}"
          headers:
            {{ $processEnvironment.AUTH_TOKEN }}: "Bearer invalid_token"
          expect:
            - statusCode: 401
            - hasProperty: "message"
            - equals:
                - "message": "Unauthorized: Invalid token"
        think: 1

      - log: "Sending a DELETE request to an incorrect URL"
        delete:
          url: "/v2/store/orders/{{ orderId }}"
          headers:
            {{ $processEnvironment.AUTH_TOKEN }}: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasProperty: "message"
            - equals:
                - "message": "Not Found: Invalid URL"
        think: 1
