# Test generated by RoostGPT for test testUnitNjs2 using AI Type Open AI and AI Model gpt-4


# Test generated for /user/{username}_put for http method type PUT in artillery framework

# RoostTestHash=f6e646c39e


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "user_username_put_success.csv"
    fields:
      - "username"
      - "id"
      - "firstName"
      - "lastName"
      - "email"
      - "password"
      - "phone"
      - "userStatus"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Update user test scenario"
    flow:
      - log: "Starting user update scenario"
      - put:
          url: "/v2/user/{{ username }}"
          headers:
            Content-Type: "application/json"
            {{ $processEnvironment.AUTH_TOKEN }}: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          json:
            id: "{{ id }}"
            username: "{{ username }}"
            firstName: "{{ firstName }}"
            lastName: "{{ lastName }}"
            email: "{{ email }}"
            password: "{{ password }}"
            phone: "{{ phone }}"
            userStatus: "{{ userStatus }}"
          expect:
            - statusCode: 200
            - hasProperty: "id"
            - hasProperty: "username"
            - hasProperty: "firstName"
            - hasProperty: "lastName"
            - hasProperty: "email"
            - hasProperty: "password"
            - hasProperty: "phone"
            - hasProperty: "userStatus"
      - think: 1
      - log: "User update scenario completed"
